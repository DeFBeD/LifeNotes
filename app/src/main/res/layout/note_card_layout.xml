<?xml version="1.0" encoding="utf-8"?>

<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="@dimen/default_4_dp"
    android:elevation="2dp"
    android:layout_margin="@dimen/default_4_dp"
    app:strokeColor="@color/note_color"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/note_color">

        <!-- TextView that displays the update date -->
        <TextView
            android:id="@+id/updateTheDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_marginStart="@dimen/default_8_dp"
            android:layout_marginTop="@dimen/default_8_dp"
            android:textColor="@android:color/primary_text_light"
            android:textSize="@dimen/text_size_12_sp"
            android:textStyle="italic"
            tools:text="11/11/1111"/>

        <TextView
            android:id="@+id/contentInNotes"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/default_8_dp"
            android:maxLines="2"
            android:textColor="@android:color/primary_text_light"
            android:textSize="@dimen/text_size_16_sp"
            android:textStyle="bold"
            tools:text="Description"/>

        <ImageView
            android:id="@+id/image_card"
            android:layout_width="@dimen/image_100_dp"
            android:layout_height="@dimen/image_100_dp"
            android:scaleType="centerCrop"
            android:layout_gravity="end"
            tools:src="@mipmap/ic_launcher"/>

    </LinearLayout>

</android.support.v7.widget.CardView>


